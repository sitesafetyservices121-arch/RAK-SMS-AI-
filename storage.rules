rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /documents/{category}/{section}/{fileName} {
      allow write: if request.auth != null;
      allow read: if true; // Public read for documents
    }
  }
}